<div class="column is-half">
    <% if user_signed_in? %>
        <article class="media box">
            <figure class="media-left">
                <figure class="image is-64x64">
            <%= gravatar_image_tag(current_user.email, size: 64, alt: current_user.name) %>
          </figure>
            </figure>
            <div class="media-content">
              
            </div>
        </article>
    <% else %>
    <% end %>
    <% @articles.each do | article | %>
    <div class="box">
        <article class="media">
            <div class="media-left">
                <figure class="image is-64x64">
            <%= gravatar_image_tag(article.user.email, size: 64, alt: article.user.name) %>
          </figure>
            </div>
            <div class="media-content">
                <div class="content">
                    <strong><%= article.user.try(:name) %></strong><br />
                    <small>@<%= article.user.try(:username) %></small><br/>
                    <p><%= article.title %></p>
                    <p><%= article.body %></p>
                </div>
                <nav class="level">
                    <div class="level-left is-mobile">
                        <%= link_to article, class: "level-item" do %>
                          <span class="icon"><i class="fa fa-link" aria-hidden="true"></i></span>
                        <% end %>
                        <%= link_to edit_article_path(article), class: "level-item" do %>
                          <span class="icon"><i class="fa fa-pencil" aria-hidden="true"></i></span>
                        <% end %>
                        <%= link_to article, method: :delete, data: { confirm: "Are you sure you want to delete this article?" } do %>
                                <span class="icon"><i class="fa fa-trash-o" aria-hidden="true"></i></span>
                        <% end %>
                    </div>
                </nav>
                <% if user_signed_in? && current_user.id == article.user_id %>
          <% end %>
            </div>
        </article>
    </div>
  <% end %>
  </div>